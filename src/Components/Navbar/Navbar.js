import {
    Nav,
    Container,
} from "./styledNavbar";

const Navbar = () => {
    return (
        <Nav>
            <Container>
                <h1>Navbar Rendering...</h1>
            </Container>
        </Nav>
    );
};

export default Navbar;
